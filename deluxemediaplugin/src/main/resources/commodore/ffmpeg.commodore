ffmpeg {
  reset;
  list-arguments;
  run;
  add {
    arguments brigadier:string greedy_phrase;
    index brigadier:integer {
      arguments brigadier:string greedy_phrase;
    }
  }
  remove {
    argument brigadier:string single_word;
    index brigadier:integer;
  }
}